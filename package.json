{
  "name": "dsh-cloud",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Multi-tenant cloud deployment for DSH.",
  "scripts": {
    "lint": "oxlint",
    "check:docs": "node scripts/check-docs.mjs",
    "check:images": "scripts/check-images.sh",
    "verify": "verify/verify.sh"
  },
  "devDependencies": {
    "oxlint": "^1.42.0"
  },
  "engines": {
    "node": "^22.19 || >=24"
  }
}
